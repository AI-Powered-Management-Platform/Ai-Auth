//! The Guard's process entry. v1 skeleton: no serving surface yet — the gRPC
//! server arrives with the generated contract code. Exists so CI builds and
//! tests the crate the services will grow inside.

fn main() {
    println!("aiauth-crypto skeleton: no serving surface yet, by design");
}
